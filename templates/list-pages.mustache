<div class="{{{class-root}}}">
    {{#pages}}
        <fieldset>
            <h3>{{{title}}}</h3>
            <div class="certificate-page-root mb-3">
                <div class="certificate-page">
                    {{{pagina}}}
                </div>
            </div>
        </fieldset>
    {{/pages}}
</div>

<style>
    fieldset {
        border: 1px solid #dee2e6;
        margin: 2px;
        padding: 0 10px 14px;
    }
    fieldset legend {
        float: none;
        margin: 0 17px;
        width: auto;
        padding: 4px;
    }
    .certificate-page-root {
        border: 4px dashed #31468c;
        display: inline-block;
    }
    .certificate-page-root .certificate-page {
        margin: 11px auto;
        padding: 2px;
        height: calc(595px + 4px);
        width: calc(842px + 4px);
        zoom: .3;
    }
    .certificate-flex-gap {
        gap: 10px
    }
</style>